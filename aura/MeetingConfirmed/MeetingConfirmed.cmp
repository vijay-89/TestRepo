<aura:component controller="MeetingConfirmed_Ctrl" implements="force:appHostable,flexipage:availableForAllPageTypes,flexipage:availableForRecordHome,force:hasRecordId,forceCommunity:availableForAllPageTypes,force:lightningQuickAction" access="global" >

    <!--<aura:handler name="init" value="{!this}" action="{!c.doInit}" />
    <aura:handler event="aura:waiting" action="{!c.waiting}" />
    <aura:handler event="aura:doneWaiting" action="{!c.doneWaiting}" />
    
    <aura:if isTrue="{!v.HideSpinner}">
        <lightning:spinner alternativeText="Loading" size="large" />
    </aura:if>-->
   <aura:attribute name="sortedDirection"
                type="Boolean"
                default="true" />
    <aura:attribute name="sortedBy" type="String"/>
    <aura:attribute type="MeetingConfirmed_Ctrl.WrapperopenEnrollment[]" name="acctList"/>
    <aura:attribute name="mycolumns" type="List"/>
     
    <aura:handler name="init" value="{!this}" action="{!c.fetchAcc}"/>
     
     <lightning:datatable data="{! v.acctList }"
                         columns="{! v.mycolumns }"
                         keyField="id"
                         sortedBy="{!v.sortedBy}"
                         sortedDirection="{!v.sortedDirection}" 
                         onsort="{!c.updateColumnSorting}" 
                         hideCheckboxColumn="true"/>
</aura:component>